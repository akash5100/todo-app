# Build the project

## you need to install the dependencies

```bash
cd server && npm install && cd ../client && npm install
```

## Build the project
    
```bash 
npm run build
```

## Run the project

```bash
npm run start
```

# Docker
